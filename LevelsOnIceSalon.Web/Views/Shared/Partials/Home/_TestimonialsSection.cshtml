@model HomePageViewModel

<section class="content-section alt-section">
    <div class="container">
        <div class="section-heading">
            <p class="eyebrow">Client Love</p>
            <h2>Clients come back for the finish, the feeling, and the standard that never slips.</h2>
        </div>
        <partial name="Partials/_TestimonialsCollection" model="Model.FeaturedTestimonials" />
    </div>
</section>
